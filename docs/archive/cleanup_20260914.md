# MCL cleanup 验收（2026-09-14）

本次降低维护和理解成本，不以磁盘占用为目标。当前入口见 [文档索引](../README.md)。

## 实施结果

- 现役保留 9 个 app；删除三个辅助 app、E01–E04 执行实现和 E04 专属 OpenSoT。
- 旧 study/campaign/continuation、补证准备/执行及一次性迁移器退役；离线 FK、指标、来源读取与 A01/A02 分析保留。
- 原生 experiment.v2 公开声明不要求迁移字段；旧声明继续检查完整来源绑定。
  prepare 使用 --definition/--output 验证并逐字节复制现役声明，不重建旧方法。
- 根 CMake 为 503 行，共享测试注册为 331 行；Step/Target solver 语义测试及 HKS 专属脚本测试归属 app。
  E01 配置阶段模型读取已删除，模型测试使用独立 MCL_TEST_URDF。
- 当前架构、运行说明和研究状态统一维护；旧设计、迁移、实施 prompt 与验收归档。
  旧 PiM 工具/布局不再安装，recorded 读取行为保留。

## 基线与保全

证据目录：`/workspace/runs/mcl_cleanup_20260914T104347Z`。

- source-before.tar.gz / source-before.json 保存任务前 2,709 个普通源码文件，包含未提交工作。
  五个未改变的源码符号链接另存 source-before-symlinks.tar / source-before-symlinks.json。
- status-before.txt、head-before.txt、working-tree-before.patch 保存 Git 基线。
- source-change-audit.json 单独比较本次任务基线，而非把原有脏树全部算成本次变更。
- 665 个受保护文件哈希保持一致，包括 PlaCo、FTXUI、baseline、SDK、原实验声明/declarations 和 HKS 算法源码。
- 四个既有冻结安装目录及依赖安装没有任务开始后的文件 mtime 变动。旧 runs 与数据没有迁移或删除。

## 验证

| 验证 | 结果 |
|---|---|
| 独立 HARP ON 构建/安装 | 9 个 app；CTest 106/106 通过 |
| 独立 HARP OFF + headless 构建/安装 | 9 个 app；CTest 88/88 通过 |
| 公开执行 Python 合同 | 14 项；最后变更后两套 execution CTest 均 2/2 通过 |
| 离线分析、补证读取和独立数值测试 | 202 项通过；删除剩余无消费者准备函数后的相关复核 46 项通过 |
| 原生声明端到端 | 准备、只读 preflight、实际求解、独立核验；正常与不可行案例均覆盖 |
| 原生失败保留 | 不可行案例 exit 1、failed；独立 validation passed 不改变原生失败 |
| 安装与加载 | 两套各 9 个 executable；无 legacy/OpenSoT 安装内容；OFF HKS 不依赖 Torch/CUDA/posture_reference |
| 文档与静态检查 | 普通文档链接无断链；现役 Python 可解析；架构检查及 diff whitespace 检查通过 |

构建使用 `/workspace/build/mcl-cleanup-on`、`/workspace/build/mcl-cleanup-headless`，
安装使用对应 `/workspace/install/mcl-cleanup-*`；依赖为既有
`/workspace/install/mcl-app-reuse-deps-20260914`。均为 RelWithDebInfo、CPU affinity OFF 的开发验证；
HARP ON 生命周期使用 `/workspace/runs/harp_phase1/models/cuda`。
Psi SDK 测试只连接 loopback fixture，不连接真实机器人。

原始日志按 configure/build/install/ctest/offline/installed 前缀保存在证据目录。
首次构建的已移动测试 include 路径错误已修复；首次聚合 pytest 意外收集 runs 内旧快照，
随后改为显式范围、排除 runs/results，并关闭不相关 ROS pytest 自动插件。
删除旧生成测试后的空测试类已修复。原始失败日志均保留。
Matplotlib 报告本机 Axes3D 不可用警告，已执行的离线测试仍通过。

## 保留的限制

这是开发合同与行为回归，不是正式 RT、求解质量研究或硬件验收。未重跑完整 campaign。
[本地证据缺项](local_evidence.md)登记 53 个文档引用的不可解析 locator；这不是科学失败率。
A01/A02 实际 dry-run 均因原 campaign plan/status/index 缺失而失败；
历史来源没有用其他 run 替代，旧原生失败和分析结论不被改写。

没有提交或推送；当前工作树包含任务前已有变更及本次 cleanup。
