# E07 输入合同

状态：`generated-development`。解析与 R1 inputs 已由本实验 generator 产生，正式冻结尚未执行。

独立保存小规模 A/b/bounds/层级/容差与预期解析性质，不保存人工填写的被测结果。R1 复用 E06 冻结模型和初始状态，按 [场景矩阵](../README.md) 产生 link4、orientation、posture references 和明确事件时间。所有 arms 读取同一 canonical 目标。

冻结 layer order、task enforcement、normalization、scale grouping、权重扫描和开关序列；primary-only 是同一场景删去次级目标的明确参考 arm。其余变量不随方法改变。

输入来源、seed、hash、时间/状态区分使用 [公共合同](../../../docs/mcc_placo_study/DATA_ANALYSIS.md)。formal 前冻结全部 cases 与验收容差；生成器不按 MCC 成绩筛场景。
